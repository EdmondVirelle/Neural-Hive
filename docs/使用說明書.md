# 🐝 Neural Hive 使用說明書

> **AI 叢集指揮中心** — 版本 1.0.0

---

## 📋 目錄

1. [簡介](#簡介)
2. [安裝與啟動](#安裝與啟動)
3. [基本操作](#基本操作)
4. [進階功能](#進階功能)
5. [配置指南](#配置指南)
6. [故障排除](#故障排除)

---

## 簡介

### 這個軟體是做什麼的？

**Neural Hive** 是一個讓您能夠**同時管理多個 AI 編程助手**的桌面應用程式。

想像一下這個場景：您正在開發一個大型專案，需要：
- 一個 AI 助手幫您重構前端程式碼
- 另一個 AI 助手幫您寫後端 API
- 第三個 AI 助手幫您撰寫測試

傳統方式下，您需要開啟多個終端機視窗，逐一切換，難以追蹤每個助手的進度。

**Neural Hive** 將所有 AI 助手集中在一個畫面，讓您：
- 📊 **一目了然** — 看到所有 Agent 的即時狀態
- 🎯 **快速切換** — 點擊卡片即可專注於特定 Agent
- 📢 **批量操作** — 一次對多個 Agent 發送相同命令

### 支援的 AI 工具

| 工具 | 支援狀態 |
|------|----------|
| Claude Code (Anthropic) | ✅ 完整支援 |
| Gemini CLI (Google) | ✅ 完整支援 |
| Aider | ✅ 完整支援 |
| 其他 CLI 工具 | ✅ 可自訂配置 |

---

## 安裝與啟動

### 第一步：安裝依賴

確保您的電腦已安裝：
- **Node.js** 18 或更高版本（[下載](https://nodejs.org)）
- **Git**（選用，用於克隆專案）

### 第二步：安裝專案

```bash
# 進入專案目錄
cd "AI Cluster Command Center"

# 安裝所有依賴
npm install
```

### 第三步：啟動應用

**開發模式**（推薦，有熱重載）：

```bash
# 終端機 1：啟動前端開發伺服器
npm run dev

# 終端機 2：啟動 Electron 應用
npm run dev:electron
```

**生產模式**：

```bash
# 建置應用
npm run build

# 啟動
npm start
```

---

## 基本操作

### 1. 主畫面介紹

啟動後，您會看到主畫面：

```
┌────────────────────────────────────────────────┐
│  🐝 Neural Hive                   [＋新增]     │
├────────────────────────────────────────────────┤
│                                                │
│   ┌──────────┐  ┌──────────┐  ┌──────────┐    │
│   │Claude-01 │  │Gemini-02 │  │ (空)     │    │
│   │🟡 思考中 │  │🟢 工作中 │  │          │    │
│   └──────────┘  └──────────┘  └──────────┘    │
│                                                │
│   狀態列: 思考中 1 | 工作中 1 | 閒置 0        │
└────────────────────────────────────────────────┘
```

### 2. 新增 Agent

1. 點擊右上角的 **[＋新增]** 按鈕
2. 選擇 Agent 類型（Claude / Gemini / Aider / 自訂）
3. (選填) 輸入 **Agent 名稱** (例如: "前端", "API 測試")
4. 設定工作目錄（Agent 將在此目錄執行）
5. 點擊 **確認** 啟動 Agent

### 3. 查看 Agent 狀態

每個 Agent 卡片顯示：
- **頂部**：Agent 名稱與類型
- **中間**：終端機預覽
- **底部**：當前工作目錄 (CWD)
- **狀態燈號**：

| 圖示 | 狀態 | 說明 |
|------|------|------|
| 🟡 | THINKING | AI 正在思考/分析 |
| 🟢 | WORKING | AI 正在執行操作（讀寫檔案等） |
| 🔵 | IDLE | AI 閒置，等待指令 |
| 🔴 | ERROR | 發生錯誤 |
| 🟠 | WAITING | 等待用戶輸入 |

### 4. 進入專注模式

**點擊任一 Agent 卡片**，即可進入專注模式：

```
┌─────────────────────────────────────────────────────────┐
│  ← 返回                            Claude-01            │
├─────────────────────────────────────────────────────────┤
│  ┌───────────────────────────┬─────────────────────┐   │
│  │                           │  🧠 思考鏈          │   │
│  │  終端機視窗               │  - 分析程式碼結構   │   │
│  │  (完整 xterm.js)          │  - 識別重構點       │   │
│  │                           │                     │   │
│  │  > Thinking...            │  🔧 已使用工具      │   │
│  │  > [Tool Use] Reading...  │  - Reading file     │   │
│  │                           │  - Searching code   │   │
│  └───────────────────────────┴─────────────────────┘   │
│  ┌─────────────────────────────────────────────────┐   │
│  │ 輸入命令: _                                     │   │
│  └─────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
```

專注模式提供：
- **左側**：完整的終端機視窗（可捲動、複製）
- **右側**：思考鏈與已使用的工具清單
- **底部**：命令輸入框

按 **Esc** 或點擊 **← 返回** 回到主畫面。

### 5. 發送命令

在專注模式下：
1. 在底部輸入框輸入命令
2. 按 **Enter** 發送

在主畫面（Agent 卡片）：
1. 點擊卡片底部的輸入區域
2. 輸入命令並按 **Enter**

### 6. 終止 Agent

1. 在 Agent 卡片上找到 **✕** 或 **終止** 按鈕
2. 點擊確認終止

---

## 進階功能

### 命令廣播

一次對多個 Agent 發送相同命令：

1. 點擊 **廣播** 按鈕（或按快捷鍵 `Ctrl+Shift+B`）
2. 選擇目標：
   - **全部 Agent** — 發送給所有正在運行的 Agent
   - **按標籤篩選** — 只發送給特定標籤的 Agent
3. 輸入命令模板
4. 點擊 **發送**

**範例**：
```
命令: npm run lint
目標: 標籤 = frontend
結果: 所有帶有 "frontend" 標籤的 Agent 都會收到 npm run lint
```

### 標籤管理

為 Agent 添加標籤，方便分組管理：

1. 右鍵點擊 Agent 卡片
2. 選擇 **編輯標籤**
3. 輸入標籤（如 `frontend`、`backend`、`testing`）
4. 儲存

### 變數注入

在廣播時為不同 Agent 注入不同的變數：

**命令模板**：
```
Check file {filename}
```

**變數設定**：
```json
{
  "filename": ["src/app.ts", "src/api.ts", "src/test.ts"]
}
```

**結果**：
- Agent 1 收到: `Check file src/app.ts`
- Agent 2 收到: `Check file src/api.ts`
- Agent 3 收到: `Check file src/test.ts`

---

## 配置指南

### 解析規則配置

解析規則定義如何從 CLI 輸出識別 Agent 狀態。

**檔案位置**：`config/parsers.json`

**範例**：

```json
{
  "parsers": {
    "claude": {
      "name": "Claude Code",
      "command": "claude",
      "patterns": {
        "thinking": "Thinking|Analyzing|Considering",
        "tool_use": "\\[Tool Use\\]|Reading|Writing",
        "error": "Error|Exception|Failed",
        "waiting": "waiting for|user input"
      }
    },
    "my_custom_tool": {
      "name": "My Custom AI",
      "command": "my-ai-cli",
      "patterns": {
        "thinking": "Processing|Loading",
        "tool_use": "Executing|Running",
        "error": "ERROR|FAILED"
      }
    }
  }
}
```

### 添加新的 AI 工具

1. 編輯 `config/parsers.json`
2. 添加新的 parser 配置
3. 重新啟動應用（或點擊 **重新載入配置**）

---

## 故障排除

### 問題：Agent 無法啟動

**可能原因**：
- CLI 工具未安裝或不在 PATH 中
- 工作目錄不存在

**解決方法**：
1. 確認 CLI 工具已安裝：`claude --version` 或 `gemini --version`
2. 確認工作目錄存在且可存取

### 問題：狀態顯示不正確

**可能原因**：
- 解析規則與 CLI 輸出格式不匹配

**解決方法**：
1. 進入專注模式，觀察實際輸出
2. 編輯 `config/parsers.json`，調整正則表達式
3. 點擊 **重新載入配置**

### 問題：應用程式卡頓

**可能原因**：
- 運行太多 Agent（超過 20 個）
- 系統資源不足

**解決方法**：
1. 終止不需要的 Agent
2. 關閉其他消耗資源的應用程式

### 問題：終端機顯示亂碼

**可能原因**：
- 終端機編碼不匹配

**解決方法**：
1. 確認系統使用 UTF-8 編碼
2. 重新啟動 Agent

---

## 快捷鍵

| 快捷鍵 | 功能 |
|--------|------|
| `Ctrl+N` | 新增 Agent |
| `Esc` | 退出專注模式 |
| `Ctrl+Shift+B` | 開啟廣播面板 |
| `Ctrl+K` | 終止當前 Agent |

---

## 技術支援

如有問題，請提交 Issue 或聯繫開發團隊。

---

**Neural Hive v1.0.0**  
*讓 AI 助手叢集化工作*
