[English](./README.md)

# 🐝 Neural Hive

> **AI Agent 叢集指揮中心** — 同時管理多個 AI 編程助手的桌面應用程式

[![Electron](https://img.shields.io/badge/Electron-35-47848F?logo=electron)](https://electronjs.org)
[![Vue 3](https://img.shields.io/badge/Vue-3.5-4FC08D?logo=vue.js)](https://vuejs.org)
[![TypeScript](https://img.shields.io/badge/TypeScript-5.5-3178C6?logo=typescript)](https://typescriptlang.org)
[![License](https://img.shields.io/badge/License-MIT-yellow.svg)](LICENSE)

> [!NOTE]
> 🚧 **專案狀態：Beta 測試中** 🚧
>
> 歡迎查看我們的 **[開發藍圖 (Roadmap)](docs/ROADMAP.zh-TW.md)** 來了解最新開發進度與未來規劃！

---

## 📖 這是什麼？

**Neural Hive** 是一個桌面應用程式，讓您能夠**同時管理多個 AI 編程助手**（如 Claude Code、Gemini CLI、Aider 等），並透過統一的介面監控它們的狀態、查看輸出、發送命令。

### 🎯 解決的問題

當您需要同時使用多個 AI 助手處理不同任務時：
- ❌ 開啟多個終端機視窗，難以追蹤
- ❌ 無法快速了解每個助手的工作狀態
- ❌ 需要逐一切換視窗發送相同命令
- ❌ 無法一目了然看到「誰在思考、誰在工作、誰遇到錯誤」

**Neural Hive** 將所有 AI 助手集中在一個畫面，提供：
- ✅ **狀態總覽** — 一眼看到所有 Agent 的狀態
- ✅ **專注模式** — 點擊任一 Agent 進入全螢幕終端機
- ✅ **批量操作** — 一次對多個 Agent 發送相同命令
- ✅ **智能解析** — 自動識別「思考中」「工作中」「錯誤」狀態

---

## 🖼️ 功能預覽

```
┌─────────────────────────────────────────────────────────────┐
│  🐝 Neural Hive                              [＋] [設定]    │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐         │
│  │ Claude-01   │  │ Gemini-02   │  │ Aider-03    │         │
│  │ 🟡 THINKING │  │ 🟢 WORKING  │  │ 🔵 IDLE     │         │
│  │             │  │             │  │             │         │
│  │ Analyzing   │  │ Reading     │  │ Waiting...  │         │
│  │ codebase... │  │ files...    │  │             │         │
│  └─────────────┘  └─────────────┘  └─────────────┘         │
│                                                             │
│  狀態: 🟡 1 思考中 | 🟢 1 工作中 | 🔵 1 閒置 | 🔴 0 錯誤   │
└─────────────────────────────────────────────────────────────┘
```

---

## 🏛️ 系統架構 (Architecture)

Neural Hive 使用基於 Electron 的架構，將核心邏輯（主進程）與 UI（渲染進程）分離。它透過一個偽終端機 (node-pty) 與外部命令列工具對接，以確保完全的相容性。

```
┌─────────────────────────────────────────────────────────────┐
│                    Electron Application                      │
├─────────────────────────────┬───────────────────────────────┤
│      Main Process           │       Renderer Process        │
│  ┌───────────────────────┐  │  ┌─────────────────────────┐  │
│  │  Node.js + TypeScript │  │  │   Vue 3 + TypeScript    │  │
│  │  ─────────────────────│  │  │  ─────────────────────  │  │
│  │  • node-pty (TTY)     │  │  │  • Pinia (State Mgmt)   │  │
│  │  • Process Manager    │◄─┼──┤  • Tailwind CSS         │  │
│  │  • Regex Parser       │──┼─►│  • Shadcn/ui            │  │
│  │  • IPC Handler        │  │  │  • xterm.js (Terminal)  │  │
│  └───────────────────────┘  │  └─────────────────────────┘  │
└─────────────────────────────┴───────────────────────────────┘
                              │
                              ▼
              ┌───────────────────────────────┐
              │     External CLI Processes    │
              │  ┌─────────┐    ┌──────────┐  │
              │  │ Claude  │    │  Gemini  │  │
              │  └─────────┘    └──────────┘  │
              └───────────────────────────────┘
```

---

## ✨ 核心功能

### 1️⃣ Agent 會話管理
- **新增 Agent** — 啟動 Claude、Gemini、Aider 或自訂 CLI 工具。
- **自訂命名** — 為每個 Agent 指定有意義的名稱（如 "前端"、"後端"），以便更好地組織管理。
- **多實例支援** — 同時運行 20+ 個 Agent。
- **生命週期控制** — 對任意 Agent 進行啟動、暫停、重啟和終止。

### 2️⃣ 完整的終端機互動
- **真實 PTY 模擬** — 欺騙 CLI 工具使其認為在真實終端機中運行，從而保留顏色、互動式提示 (y/n) 和游標移動。
- **專注模式** — 點擊卡片進入全螢幕終端機，配有專屬的「思考鏈」和「已用技能」面板。
- **上下文感知** — 在卡片底部隨時顯示 Agent 的當前工作目錄 (CWD)。

### 3️⃣ 智能狀態解析
- **豐富的狀態偵測** — 自動識別 `IDLE`、`THINKING`、`WORKING`、`ERROR`，甚至包括用於互動式提示的 `WAITING_USER` 狀態。
- **技能追蹤** — 智能提取 Agent 正在使用的工具（例如 "Reading file...", "Searching..."）並顯示出來。

### 4️⃣ 精緻且響應式的 UI/UX
- **視覺化回饋** — 卡片邊框的「呼吸燈」效果讓您清楚看到哪個 Agent 正在工作中。
- **即時錯誤警報** — 如果發生錯誤，Agent 卡片會變紅，並且工作列圖示上會出現標記。
- **響應式佈局** — 網格會根據您的視窗大小自動調整欄數。

### 5️⃣ 強大的自動化功能
- **指令廣播** — 使用彈性的標籤系統，將同一個指令一次發送給多個 Agent。
- **變數注入** — 在廣播的指令中使用變數，以實現動態提示（例如 `Analyze {file_path}`）。

### 6️⃣ 高擴充性與穩定性的設計
- **設定檔驅動的解析器** — 只需編輯 JSON 檔案即可自行增加對新 AI CLI 工具的支援，無需修改程式碼。
- **高效能渲染** — 內建的輸出節流器可防止在 Agent 產生大量文字時 UI 凍結，確保流暢體驗。

---

## 🚀 快速開始

### 系統需求

- **Node.js** 18.x 或更高版本
- **npm** 9.x 或更高版本
- **Python** 3.x (可選, 用於執行 `execution` 中的腳本)
- **作業系統** Windows 10+、macOS 10.15+、Linux

### 安裝

```bash
# 1. 克隆專案
git clone https://github.com/your-org/neural-hive.git
cd neural-hive

# 2. 安裝 Node.js 依賴
npm install

# 3. (可選) 安裝 Python 依賴
# 如果 execution/parse_lint.py 需要額外套件，建議建立 requirements.txt
# pip install -r execution/requirements.txt
```

### 啟動 Electron 應用

```bash
# 開發模式（熱重載）
npm run dev          # 啟動 Vite 開發伺服器
npm run dev:electron # 啟動 Electron（另開終端）

# 生產建置
npm run build        # 建置前端
npm run build:electron # 打包 Electron 應用
```

---

## 📁 專案結構

```
neural-hive/
├── electron/                 # Electron 主程序
│   ├── main.ts              # 主進程入口
│   ├── preload.ts           # 預載腳本（IPC Bridge）
│   ├── config-loader.ts     # 解析規則載入器
│   ├── output-throttler.ts  # 輸出節流器（100ms）
│   ├── broadcast-manager.ts # 命令廣播管理器
│   └── __tests__/           # 後端測試
│
├── src/                      # Vue 前端
│   ├── App.vue              # 主應用組件
│   ├── main.ts              # Vue 入口
│   ├── components/          # UI 組件
│   │   ├── AgentCard.vue    # Agent 卡片
│   │   ├── AgentGrid.vue    # Agent 網格佈局
│   │   ├── FocusMode.vue    # 專注模式（全螢幕）
│   │   ├── TerminalView.vue # xterm.js 終端機
│   │   └── SkillsPanel.vue  # 技能/思考鏈面板
│   ├── composables/
│   │   └── useTerminal.ts   # xterm.js 整合
│   ├── stores/
│   │   └── agentStore.ts    # Pinia 狀態管理
│   ├── utils/
│   │   └── parser.ts        # 輸出解析工具
│   └── __tests__/           # 前端測試
│
├── config/
│   └── parsers.json         # 解析規則配置
│
├── scripts/
│   └── mock-agent.js        # 模擬 Agent（開發用）
│
├── package.json
├── vite.config.ts
├── tailwind.config.js
├── tsconfig.json
└── vitest.config.ts
```

---

## ⚙️ 配置

### 解析規則 (`config/parsers.json`)

定義如何從 CLI 輸出識別 Agent 狀態：

```json
{
  "parsers": {
    "claude": {
      "name": "Claude Code",
      "command": "claude",
      "patterns": {
        "thinking": "Thinking|Analyzing|Considering",
        "tool_use": "\\[Tool Use\\]|Reading|Writing|Searching",
        "error": "Error|Exception|Failed"
      }
    }
  }
}
```

### 環境變數

| 變數 | 說明 | 預設值 |
|------|------|--------|
| `NODE_ENV` | 環境模式 | `development` |
| `VITE_PORT` | 開發伺服器埠號 | `5173` |

---

## 🧪 測試

```bash
# 執行所有測試
npm test

# 執行測試（單次，不監聽）
npm test -- --run

# 執行測試並產生覆蓋率報告
npm run test:coverage
```

### 測試覆蓋

| 模組 | 測試數 |
|------|--------|
| `parser.ts` | 31 |
| `agentStore.ts` | 21 |
| `output-throttler.ts` | 15 |
| `broadcast-manager.ts` | 25 |
| `config-loader.ts` | 20 |
| **總計** | **112** |

---

## 🔧 開發指南

### 新增 Agent 類型

1. 編輯 `config/parsers.json`，添加新的解析規則
2. 更新 `src/types/shared.ts` 中的 `AgentType`
3. 更新 `src/stores/agentStore.ts` 中的 `generateName`

### 新增 IPC 通道

1. 在 `src/types/shared.ts` 中添加通道名稱
2. 在 `electron/main.ts` 中實作 handler
3. 在 `electron/preload.ts` 中暴露 API
4. 在前端呼叫 `window.electronAPI.xxx()`

---

## 📜 常見問題

### Q: 為什麼 Agent 卡片顯示 "未知狀態"？

A: 檢查 `config/parsers.json` 中的正則表達式是否能匹配該 CLI 工具的輸出格式。

### Q: 如何支援新的 AI CLI 工具？

A: 在 `config/parsers.json` 中添加新的 parser 配置，定義該工具的輸出模式。

### Q: 可以同時運行多少個 Agent？

A: 設計上支援 20+ 個並發 Agent，實際取決於系統資源。

---

## 🛡️ 安全性

- ✅ Electron 沙盒模式 (`sandbox: true`)
- ✅ 上下文隔離 (`contextIsolation: true`)
- ✅ 禁用 Node 整合 (`nodeIntegration: false`)
- ✅ 完整的輸入驗證
- ✅ 無硬編碼敏感資訊

---

## 📄 授權

MIT License

---

## 🤝 貢獻

歡迎提交 Issue 和 Pull Request！

---

<p align="center">
  <b>Neural Hive</b> — 讓 AI 助手叢集化工作<br>
  Made with ❤️ by the Neural Hive Team
</p>
